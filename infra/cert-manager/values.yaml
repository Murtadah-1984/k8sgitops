# Bitnami cert-manager Helm Values
# Production configuration with Ceph storage

global:
  storageClass: "rook-ceph-block"

installCRDs: true

extraArgs:
  - --enable-certificate-owner-ref=true

prometheus:
  enabled: false  # enable if you have Prometheus

webhook:
  timeoutSeconds: 30

